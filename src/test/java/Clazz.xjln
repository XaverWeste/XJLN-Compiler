use java/lang/String as String
use java/lang/Object as Object

def clazz[int i, int j] -> init()

    inner String s
    inner const var v
    {String | char} s

    def init()

    end

    def test(int i)
        i = 10
        if i == 10
            i = i / 2
        end
    end

end