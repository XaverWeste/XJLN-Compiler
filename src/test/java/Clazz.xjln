use {String, Object} from java/lang

main
    i = 5
end

def Class[int i, double d]

    long l

    def Method(int j) :: int
        while j > 0
            j = j - 1
            Method(j)
        end
        return 0
    end

    #inner const [int] = [4, 3]
end