use java/lang/System

main -> System:out:println("Hello World!")

def Clazz[]

    [[int]] intArray

    def test() -> intArray = [int, 3, 4]

end

############# syntax preview ###################
#
#def Class[/] => java/lang/Object, java/lang/Class
#
#   [[int]] intArray = [int, 4, 3]
#
#   def fib(int n) :: int{
#       (0) = 1
#       (1) = 1
#       (n > 0) = fib(n - 1) + fib(n - 2)
#   }
#
#end
#
#def someThing = (int i, int j)