use java/lang/System

main -> System:out:println("Hello World!")

############# syntax preview ###################
#
#def Class[/] => java/lang/Object, java/lang/Class
#
#   i = 5
#
#   def fib(int n) :: int{
#       (0) = 1
#       (1) = 1
#       (n > 0) = fib(n - 1) + fib(n - 2)
#   }
#
#end

# Roadmap
#
# 0.6 method overloading, super classes (and dynamic typing part 1)
# 0.7 dynamic typing part 2 (of 2) and advanced syntax for method definition
# 0.8 interfaces and static methods and fields
# 0.9 optional parameter, generics
# 1.0 bug fixes, general improvements and standard library