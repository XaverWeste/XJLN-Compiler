use System from java/lang as system

main
    system:out:println(0)
end

############# feature preview ###################

#def class[/] => java/lang/Object, java/lang/Class

#   [int] intArray = [4, 3]

#   def function() :: int = 0

#   def function(someClass sc) -> intArray:[1,2] = sc:intValue()

#   def fib(int n) :: int {
#       (0) = 0
#       (1) = 0
#       (n > 1) = fib(n - 1) - fib(n - 2)
#   }

#end

#def someThing = (int i, int j)