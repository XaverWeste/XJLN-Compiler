use {String, Object} from java/lang
use java/lang/System

main
    Clazz[0,0.0]
end

def Clazz[int i, double d]

    long l

    def Method(int j) :: int
        while j > 0
            j = j - 1
            Method(j)
        end
        return 0
    end

    #inner const [int] = [4, 3]
end