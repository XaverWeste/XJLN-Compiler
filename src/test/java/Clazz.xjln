use System from java/lang as system

main
    #system:out:println(0)
    Clazz c = Clazz[]
end

def Clazz[]

    def inner test(int i) :: int = i * 2

end

############# feature preview ###################

#def Class[/] => java/lang/Object, java/lang/Class

#   [int] intArray = [4, 3]

#   def fib(int n) :: int {
#       (0) = 0
#       (1) = 0
#       (n > 1) = fib(n - 1) - fib(n - 2)
#   }

#end

#def someThing = (int i, int j)